# Backup images — large raw dumps, never commit these.
backup/
images/
*.img
!device/dxe/*.ffs

# Build trees
work/
/out/
mu_aloha_platforms/
Mu-Silicium/
edk2/
workspace/

# Python
__pycache__/
*.pyc
.venv/

# UEFI platform package output — regenerated from device/dxe by
# tools/make_xbl_binaries.py, which is why the blobs are not duplicated here.
uefi/Binaries/
